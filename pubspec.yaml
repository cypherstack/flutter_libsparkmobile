name: flutter_libsparkmobile
description: Flutter Sparkmobile ffi wrapper
version: 0.2.0
homepage: none

environment:
  sdk: '>=3.13.0 <4.0.0'
  flutter: ">=3.47.2"

dependencies:
  code_assets: ^1.2.1
  ffi: ^2.1.0
  flutter:
    sdk: flutter
  hooks: ^2.0.2
  native_toolchain_cmake: ^0.3.2

dev_dependencies:
  ffigen: ^21.0.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
